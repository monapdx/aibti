# CMLX · 卡点求助侠 - 5 Cases

### Case 1: 日常工作（小 bug 求助）
**场景**: 写接口时冒出 NPE，不知道从哪下手。
**Prompt**:
```
user.getName() 这行报 NPE 咋办，user 明明查出来了
```
**四维判定**:
- A/C: **C**（贴了具体代码行和报错名）
- M/V: **M**（只管这一行崩，没拉架构）
- D/L: **L**（"咋办"求下位指令）
- X/E: **X**（先问怎么绕，没追根因）
- **最终代码**: CMLX
**为什么典型**: 对比 CMDX 会补一串 stack + 自己的假设去追问，CMLX 只扔代码行+"咋办"；对比 CMLE 会说"我们一起定位下"，CMLX 是单向求救；对比 AMLX 会抽象成"空指针哲学"，CMLX 紧贴这一行。

---

### Case 2: 压力/紧迫（线上告警求救）
**场景**: 凌晨线上 5xx 飙升，值班人慌了。
**Prompt**:
```
救命 线上 502 暴涨 nginx 日志 upstream timed out 咋办急
```
**四维判定**:
- A/C: **C**（502 / upstream timed out 具体错）
- M/V: **M**（只看超时这一个点）
- D/L: **L**（"救命""咋办急" 明显求救）
- X/E: **X**（先问怎么先止血）
- **最终代码**: CMLX
**为什么典型**: CMDX 这时会贴 QPS、连接数、nginx.conf 片段追问根因；CMLE 会"我来看连接池，你看下游"分工；AMLX 会感叹"又是凌晨的报警"；CMLX 就一个字——"急"。

---

<!-- revised in P0 fix: 原 "npm ERESOLVE" case 与 Case 1/4 同质（都是报错求救），而且让 CMLX 硬凑"创意"。改成边界场景——新人对选型的迷茫式求助，依然短具象 + 低位求救 + 探索，但话题从 bug 上移到选型，测 LLM 能否在"求助话题变抽象"时仍判对 CMLX -->
### Case 3: 边界场景（新人对技术选型的迷茫式求助）
**场景**: 新人刚入职，看 RFC 看不懂，对学长发起低位求救。
**Prompt**:
```
学长问下 我们为啥选 Kafka 不用 Pulsar 文档看懵了 完全没头绪
```
**四维判定**:
- A/C: **C**（指名 `Kafka` / `Pulsar` 两个具体中间件，不是泛谈"消息队列"）
- M/V: **M**（30 字内，不铺业务背景也不贴对比表）
- D/L: **L**（"学长问下""看懵了""完全没头绪"连发三个求救信号）
- X/E: **X**（要的是"为啥选"的理解，不要"立刻改方案"）
- **最终代码**: CMLX

**为什么典型（边界如何不破）**:
- 话题上移到选型，**看起来像要开放讨论**，但语气完全没抬起来——"看懵了/没头绪"依然是 L 的蹲姿。
- vs CMDX："Pulsar 分层存储明明更优，为啥没普及？" —— CMDX 自带假设和证据反问，是站着问；CMLX 是蹲着问。
- vs CMLE："学长咱俩梳下这次选型的账吧" —— CMLE 提出协作共做，CMLX 只要个答案。
- vs AMLX："消息队列的选型其实是一种世界观啊" —— AMLX 会往抽象飘，CMLX 死死咬住"Kafka vs Pulsar"这个具体坐标。

---

### Case 4: 失败/卡壳（完全卡死求援）← 核心场景
**场景**: 本地 docker build 卡在某一步不动，什么线索都没有。
**Prompt**:
```
docker build 卡在 step 7/12 半小时没动 啥都没输出 救命
```
**四维判定**:
- A/C: **C**（step 7/12 精确坐标）
- M/V: **M**（只管这一步，不铺全貌）
- D/L: **L**（"救命" + 放弃自查）
- X/E: **X**（先问怎么破局）
- **最终代码**: CMLX
**为什么典型**: 这是 CMLX 最纯的形态——有具体坐标（step 7/12），但完全没方向。CMDX 会 `docker build --progress=plain` 贴日志追问；CMLE 会"我这边也试一下同样的 Dockerfile"结伴；AMLX 会"构建系统为啥这么慢"发散；CMLX 直接"救命"。

---

### Case 5: 复杂多轮对话（求助后新问题又来）
**场景**: 刚按指引改完，又冒出新错，接着求。
**Prompt**:
```
按你说的加了 @Transactional 现在报 no session 咋整 又卡住了
```
**四维判定**:
- A/C: **C**（`@Transactional` + "no session" 具体）
- M/V: **M**（只管当前这个新错）
- D/L: **L**（"咋整""又卡住了"持续下位）
- X/E: **X**（想先过这关）
- **最终代码**: CMLX
**为什么典型**: 多轮里 CMLX 依旧是"接一个错就问一个错"的求救节奏。CMDX 会"我怀疑是 session-in-view 关了，对吗"带假设；CMLE 会"我们回头看看事务边界是不是设错层了"回溯共担；AMLX 会"事务和会话到底是啥关系"抽象化；CMLX 只会"又卡住了 咋整"。
